<div class="jumbotron">
  <div class="container">
    <h1>Brutus Blog</h1>
  </div>
</div>

<div class="container">
  {{outlet}}
</div>

{{#each model as |post|}}
  <article>
    <header class="page-header">
      <h2>{{post.title}}</h2>
    </header>
    <p>{{post.body}}</p>
  </article>
{{/each}}
